apiVersion: v1
kind: Service
metadata:
  name: release-service
  namespace: release-lab
spec:
  selector: 
    app: release-app
    strategy: rolling
  ports:
    - name: http
      port: 80
      targetPort: 5000
  type: NodePort
  